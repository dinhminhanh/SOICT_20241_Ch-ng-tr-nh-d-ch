(* Hay cho biet chuong trinh sau day gay ra loi gi? Trong bao cao can giai thich tai sao lai co loi nhu vay dua tren code cua bo kiem tra kieu*)
program semantics1;
	const d = 1;
	var	a: integer;
		b: integer;
		c: integer;
	function Q(var x: integer; y: integer): integer;
		begin
			x:= 50;
			y:= 60;
			Q:= 100;
		end;
	begin
		a:=500;
		b:=600;
		c:=Q(d,b);
		call WRITEI (a);
		call WRITELN;
		call WRITEI (b);
		call WRITELN;
		call WRITEI (c);
	end.
